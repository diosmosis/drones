interface DirectiveScope_{{ interface.getDirective().getDirectiveName() }} {
    {% for property in interface.getDirective().getScopeProperties() %}
    {% if property.hasBidirectionalBinding() %}
    {{ property.type }} get_{{ property.name }}();
    void set_{{ property.name }}({{ property.type }} value);
    {% endif %}
    {% endfor %}
}