{% if locals.getScope().getParentScope() != null %}final {{ locals.getScope().getParentScope().getScopeClassName() }} _parent = scope._parent;{% endif %}
{% for property in locals.getScope().allProperties() %}final {{ property.type }} {{ property.name }} = {{ property.accessCode() }};
{% endfor %}
