package {{ module.getGeneratedPackage() }};

import dagger.Module;
import dagger.Provides;
import android.app.Activity;
import com.flarestar.drones.base.annotations.di.ActivityScope;

@Module
class {{ module.getGeneratedClassName() }} {

    private Activity activity;

    {{ module.getGeneratedClassName() }}(Activity activity) {
        this.activity = activity;
    }

    @Provides @ActivityScope Activity provideActivity() {
        return this.activity;
    }
}
